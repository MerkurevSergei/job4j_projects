package ru.job4j.tracker;

import org.junit.Test;

public class StartUITest {

    @Test
    public void whenInit() {
//        AnnotationConfigApplicationContext context = new AnnotationConfigApplicationContext();
//
//        Input input = Mockito.mock(Input.class);
//        Store tracker = Mockito.mock(Store.class);
//
//        context.register(input.getClass());
//        context.register(tracker.getClass());
//        context.register(StartUI.class);
//        context.refresh();
//
//        input = context.getBean(input.getClass());
//        tracker = context.getBean(tracker.getClass());
//
//        Mockito.when(input.askInt("Select: ")).thenReturn(0);
//        final UserAction action = Mockito.mock(UserAction.class);
//        Mockito.when(action.execute(input, tracker)).thenReturn(false);
//
//        context.getBean(StartUI.class).init(new UserAction[]{action});
//
//        Mockito.verify(input, times(1)).askInt("Select: ");
//        Mockito.verify(action, times(1)).execute(input, tracker);

    }
}